--- a/index.html
+++ b/index.html
@@ -1530,7 +1530,7 @@
 (function(){
   "use strict";
   const STORE_KEY = "installments-ledger-v1";
-  const APP_VERSION = "3.10.2";
+  const APP_VERSION = "3.10.3";
   const APP_SCHEMA_VERSION = 3;
   const SYNC_META_KEY = "installments-sync-meta-v1";
   const RECOVERY_KEY = "installments-recovery-snapshot-v1";
@@ -4408,8 +4408,7 @@
     pushUndoSnapshot();
     includedParts.forEach(k => { state[k] = clean[k]; });
     if (hasCards){
-      saveCardsBlob(data.cardsBlob, !fromRemote);
-      cardsPlain = null; sessionPassphrase = null; revealed = {};
+      restoreCardsBlobFromPayload(data.cardsBlob, !fromRemote);
     }
     save(!fromRemote); render();
     const summary = (includedNames ? includedNames + (hasCards ? ' و کارت‌های بانکی' : '') : (hasCards ? 'کارت‌های بانکی' : ''));
@@ -4431,6 +4430,13 @@
     return IS_GITHUB_PAGES ? `./${file}?${q}` : `${UPDATE_RAW_BASE}${file}?${q}`;
   }
   const RELEASE_HISTORY = [
+    {version:'3.10.3',released:'2026-09-17',title:'بازیابی مطمئن کارت‌های بانکی پس از Pull',changes:[
+      'کارت‌های رمزنگاری‌شده موجود در GitHub Backup پس از Pull به‌صورت واضح به‌عنوان بازیابی‌شده نمایش داده می‌شوند.',
+      'اگر کارت‌ها در همان جلسه باز بوده باشند، برنامه با همان رمز موجود در حافظه تلاش می‌کند Blob بازیابی‌شده را خودکار باز کند.',
+      'اگر بازکردن خودکار ممکن نباشد، کارت‌ها محفوظ می‌مانند و پیام روشن برای واردکردن رمز نمایش داده می‌شود.',
+      'نتیجه Pull صراحتاً اعلام می‌کند که کارت‌های بانکی رمزنگاری‌شده نیز بازیابی شده‌اند.',
+      'Import مستقل کارت‌ها نیز از همان مسیر بازیابی امن استفاده می‌کند؛ رمز کارت‌ها همچنان در Backup یا GitHub ذخیره نمی‌شود.'
+    ]},
     {version:'3.10.2',released:'2026-09-17',title:'پرداخت ساختاریافته، کانال بانکی و یکپارچگی هزینه‌ها',changes:[
       'تفکیک روش پرداخت از کانال انجام؛ اینترنت‌بانک و موبایل‌بانک به‌عنوان کانال مستقل ثبت می‌شوند.',
       'مبلغ و شناسه هر قسط هنگام پرداخت قابل اصلاح است؛ مناسب اقساط با مبالغ متفاوت.',
@@ -4623,7 +4629,8 @@
       const cfg2=loadGithubConfig(); cfg2.lastSync=todayFullDateLine(); cfg2.lastRemoteSha=json.sha; cfg2.lastSyncDirection='pull'; saveGithubConfig(cfg2);
       if(pendingBefore===0) clearLocalDirty(); else { const m=loadSyncMeta(); m.conflict=false; saveSyncMeta(m); }
       updateGithubStatusLine(); connectivityState='github'; updateConnectivityPill();
-      syncPanelShowResult(true,'دریافت موفق',`<span class="sp-ok">با موفقیت دریافت شد ✓</span><br>شامل: ${summary}<br><span style="color:var(--ink-soft);">Snapshot بازیابی قبل از Pull نگهداری شد.</span>`);
+      const cardsNote = data.cardsBlob ? '<br><span style="color:var(--ink-soft);">کارت‌های رمزنگاری‌شده هم بازیابی شدند؛ اگر قفل باشند، رمز کارت‌ها را وارد کنید.</span>' : '';
+      syncPanelShowResult(true,'دریافت موفق',`<span class="sp-ok">با موفقیت دریافت شد ✓</span><br>شامل: ${summary}${cardsNote}<br><span style="color:var(--ink-soft);">Snapshot بازیابی قبل از Pull نگهداری شد.</span>`);
     }catch(err){ syncPanelShowResult(false,'دریافت ناموفق',`<span class="sp-err">اطلاعات محلی دست‌نخورده باقی ماند.</span><br>${escapeHtml(err&&err.message?err.message:'خطای نامشخص')}`); }
   }
   document.getElementById('btnGithubPull').onclick = githubPull;
@@ -4874,6 +4881,7 @@
   const CARDS_KEY = "installments-cards-blob-v1";
   let cardsPlain = null;        // decrypted array, only in memory while unlocked
   let sessionPassphrase = null; // kept in memory only while unlocked
+  let cardsRestorePendingNotice = false; // true after Backup/Pull restores a locked card blob
   let editingCardId = null;
   let revealed = {};            // per-card reveal toggle (session only)
 
@@ -4945,6 +4953,28 @@
     catch(e){ return null; }
   }
   function saveCardsBlob(blob, markDirty = true){ localStorage.setItem(CARDS_KEY, JSON.stringify(blob)); if(markDirty) markLocalDirty(); }
+  function restoreCardsBlobFromPayload(blob, markDirty){
+    const previousPass = sessionPassphrase;
+    saveCardsBlob(blob, markDirty);
+    cardsPlain = null; revealed = {};
+    cardsRestorePendingNotice = true;
+    if (previousPass){
+      decryptCards(previousPass, blob).then(arr => {
+        cardsPlain = Array.isArray(arr) ? arr : [];
+        sessionPassphrase = previousPass;
+        cardsRestorePendingNotice = false;
+        render();
+        toast(`${toFa(cardsPlain.length)} کارت بانکی از بکاپ بازیابی و باز شد`);
+      }).catch(() => {
+        sessionPassphrase = null;
+        render();
+        toast('کارت‌های بانکی بازیابی شدند — برای مشاهده، رمز کارت‌ها را وارد کنید');
+      });
+    } else {
+      sessionPassphrase = null;
+      setTimeout(() => toast('کارت‌های بانکی بازیابی شدند — برای مشاهده، رمز کارت‌ها را وارد کنید'), 0);
+    }
+  }
   async function persistCards(){
     const blob = await encryptCards(sessionPassphrase, cardsPlain);
     saveCardsBlob(blob);
@@ -4994,10 +5024,8 @@
         const blob = JSON.parse(reader.result);
         if (!blob.salt || !blob.iv || !blob.data) throw new Error('invalid');
         if (!confirm('کارت‌های بانکی فعلی (در همین دستگاه) با این فایل جایگزین می‌شوند. ادامه؟')) return;
-        saveCardsBlob(blob);
-        cardsPlain = null; sessionPassphrase = null; revealed = {};
+        restoreCardsBlobFromPayload(blob, true);
         render();
-        toast('کارت‌ها بازیابی شد — برای دیدن، رمز را وارد کنید');
       }catch(err){
         alert('فایل معتبر نیست.');
       }
@@ -5050,7 +5078,7 @@
 
     if (cardsPlain === null){
       bar.innerHTML = `
-        <span>🔒 کارت‌های بانکی قفل است.</span>
+        <span>${cardsRestorePendingNotice ? '✅ کارت‌های بانکی از بکاپ بازیابی شده‌اند و اکنون قفل هستند؛ رمز کارت‌ها را برای مشاهده وارد کنید.' : '🔒 کارت‌های بانکی قفل است.'}</span>
         <div class="lockbar-row">
           <input type="password" id="unlockPass" placeholder="گذرواژه">
           <button class="primary small" id="btnUnlockCards">باز کردن</button>
@@ -5060,7 +5088,9 @@
         try{
           cardsPlain = await decryptCards(pass, blob);
           sessionPassphrase = pass;
+          cardsRestorePendingNotice = false;
           render();
+          toast(`${toFa(cardsPlain.length)} کارت بانکی باز شد`);
         }catch(e){ alert('گذرواژه اشتباه است.'); }
       };
       document.getElementById('btnUnlockCards').onclick = doUnlock;
@@ -5071,7 +5101,7 @@
     bar.innerHTML = `<span>🔓 کارت‌های بانکی <b>باز</b> است — روی هر قسط، اطلاعات کارت‌های مرتبط دیده می‌شود.</span>
       <div class="lockbar-row"><button class="ghost small" id="btnLockCards">🔒 قفل کردن</button></div>`;
     document.getElementById('btnLockCards').onclick = () => {
-      cardsPlain = null; sessionPassphrase = null; revealed = {};
+      cardsPlain = null; sessionPassphrase = null; revealed = {}; cardsRestorePendingNotice = false;
       render();
     };
   }
