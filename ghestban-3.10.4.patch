--- a/index.html
+++ b/index.html
@@ -681,6 +681,7 @@
           <button class="ghost small" id="btnSaveGithubConfig">💾 ذخیره تنظیمات گیت‌هاب</button>
           <button class="primary small" id="btnGithubPush">⬆️ بک‌آپ / Push</button>
           <button class="ghost small" id="btnGithubPull">⬇️ دریافت / Pull</button>
+          <button class="ghost small" id="btnGithubHistory">🕘 تاریخچه بکاپ‌ها</button>
           <button class="ghost small" id="btnCheckConnectivity">📡 بررسی اتصال</button>
         </div>
         <div id="syncMetaStatusLine" class="mini-status"></div>
@@ -1142,6 +1143,29 @@
   </div>
 </div>
 
+<!-- GitHub Backup History -->
+<div class="overlay" id="backupHistoryOverlay">
+  <div class="modal" style="max-width:900px;">
+    <h3>🕘 تاریخچه بکاپ‌های GitHub</h3>
+    <div class="mini-status" id="backupHistoryStatus">برای مشاهده نسخه‌های قبلی، «بارگذاری تاریخچه» را بزنید.</div>
+    <div id="backupHistoryList" style="max-height:430px; overflow:auto; margin-top:10px;"></div>
+    <div class="modal-actions">
+      <button class="ghost" id="btnReloadBackupHistory">بارگذاری تاریخچه</button>
+      <button class="ghost" id="btnCloseBackupHistory">بستن</button>
+    </div>
+  </div>
+</div>
+
+<!-- Deleted Cards Trash -->
+<div class="overlay" id="cardTrashOverlay">
+  <div class="modal" style="max-width:760px;">
+    <h3>🗑 کارت‌های حذف‌شده</h3>
+    <div class="mini-status">کارت حذف‌شده تا وقتی «حذف قطعی» نشود داخل فایل رمزنگاری‌شده باقی می‌ماند و همراه بکاپ ذخیره می‌شود.</div>
+    <div id="cardTrashList" style="max-height:420px; overflow:auto; margin-top:10px;"></div>
+    <div class="modal-actions"><button class="ghost" id="btnCloseCardTrash">بستن</button></div>
+  </div>
+</div>
+
 <!-- Add/Edit Card Modal -->
 <div class="overlay" id="cardOverlay">
   <div class="modal">
@@ -1530,7 +1554,7 @@
 (function(){
   "use strict";
   const STORE_KEY = "installments-ledger-v1";
-  const APP_VERSION = "3.10.3";
+  const APP_VERSION = "3.10.4";
   const APP_SCHEMA_VERSION = 3;
   const SYNC_META_KEY = "installments-sync-meta-v1";
   const RECOVERY_KEY = "installments-recovery-snapshot-v1";
@@ -2844,7 +2868,7 @@
       // locked or not set up — keep quiet, single global hint lives in the lockbar
       return '';
     }
-    const cards = cardsPlain.filter(c => c.loanId === loan.id);
+    const cards = cardsPlain.filter(c => c.loanId === loan.id && !c.deletedAt);
     let inner = '';
     if (cards.length === 0){
       inner = '<div class="lc-empty">کارتی برای این قسط ثبت نشده.</div>';
@@ -4430,6 +4454,13 @@
     return IS_GITHUB_PAGES ? `./${file}?${q}` : `${UPDATE_RAW_BASE}${file}?${q}`;
   }
   const RELEASE_HISTORY = [
+    {version:'3.10.4',released:'2026-09-17',title:'تاریخچه بکاپ و سطل زباله کارت‌ها',changes:[
+      'نمایش ۱۵ نسخه اخیر بکاپ GitHub از داخل برنامه و امکان بازیابی فقط کارت‌ها از هر نسخه.',
+      'بازیابی کارت‌ها از تاریخچه بدون دست‌زدن به اقساط، درآمدها، هزینه‌ها و حساب‌های بانکی فعلی.',
+      'حذف کارت به‌صورت Soft Delete و انتقال به سطل زباله رمزنگاری‌شده به‌جای حذف قطعی فوری.',
+      'امکان بازیابی کارت حذف‌شده یا حذف قطعی آن از سطل زباله.',
+      'کارت‌های حذف‌شده همراه cardsBlob رمزنگاری‌شده در بکاپ GitHub باقی می‌مانند تا بازیابی ساده باشد.'
+    ]},
     {version:'3.10.3',released:'2026-09-17',title:'بازیابی مطمئن کارت‌های بانکی پس از Pull',changes:[
       'کارت‌های رمزنگاری‌شده موجود در GitHub Backup پس از Pull به‌صورت واضح به‌عنوان بازیابی‌شده نمایش داده می‌شوند.',
       'اگر کارت‌ها در همان جلسه باز بوده باشند، برنامه با همان رمز موجود در حافظه تلاش می‌کند Blob بازیابی‌شده را خودکار باز کند.',
@@ -4635,6 +4666,54 @@
   }
   document.getElementById('btnGithubPull').onclick = githubPull;
 
+  const backupHistoryOverlay=document.getElementById('backupHistoryOverlay');
+  function githubHeaders(cfg){ return {Authorization:`Bearer ${cfg.token}`,Accept:'application/vnd.github+json'}; }
+  async function loadGithubBackupHistory(){
+    const cfg=loadGithubConfig();
+    const status=document.getElementById('backupHistoryStatus'), list=document.getElementById('backupHistoryList');
+    if(!cfg.repo||!cfg.token){ status.className='mini-status warn'; status.textContent='ابتدا تنظیمات GitHub و توکن را ذخیره کنید.'; list.innerHTML=''; return; }
+    status.className='mini-status'; status.textContent='در حال دریافت تاریخچه بکاپ‌ها...'; list.innerHTML='';
+    try{
+      const u=`https://api.github.com/repos/${cfg.repo}/commits?sha=${encodeURIComponent(cfg.branch||'main')}&path=${encodeURIComponent(cfg.path||'qestban-data.json')}&per_page=15`;
+      const r=await fetchWithTimeout(u,{headers:githubHeaders(cfg),cache:'no-store'},10000);
+      if(!r.ok){ const ej=await r.json().catch(()=>({})); throw new Error(r.status+' '+(ej.message||'')); }
+      const commits=await r.json();
+      if(!Array.isArray(commits)||!commits.length){ status.className='mini-status warn'; status.textContent='تاریخچه‌ای برای فایل بکاپ پیدا نشد.'; return; }
+      status.className='mini-status ok'; status.textContent=`${toFa(commits.length)} نسخه اخیر پیدا شد. بازیابی کارت‌ها فقط cardsBlob همان نسخه را جایگزین می‌کند.`;
+      list.innerHTML=commits.map((c,i)=>{
+        const dt=c.commit&&c.commit.committer&&c.commit.committer.date ? new Date(c.commit.committer.date).toLocaleString('fa-IR') : '';
+        const msg=(c.commit&&c.commit.message)||'بکاپ';
+        return `<div style="padding:10px 0; border-bottom:1px dashed var(--paper-line); display:flex; gap:10px; align-items:center; flex-wrap:wrap;">
+          <div style="flex:1; min-width:280px;"><b>${i===0?'نسخه فعلی':'نسخه قبلی'}</b> · <span class="mono">${escapeHtml(String(c.sha||'').slice(0,8))}</span><div style="font-size:11px;">${escapeHtml(msg)}</div><div style="font-size:10px; color:var(--ink-soft);">${escapeHtml(dt)}</div></div>
+          <button class="ghost small" data-history-action="restore-cards" data-sha="${escapeHtml(c.sha)}">💳 بازیابی فقط کارت‌ها</button>
+        </div>`;
+      }).join('');
+    }catch(err){ status.className='mini-status warn'; status.textContent='دریافت تاریخچه ناموفق بود: '+((err&&err.message)||'خطای نامشخص'); }
+  }
+  async function restoreCardsFromHistory(commitSha){
+    const cfg=loadGithubConfig(); if(!cfg.repo||!cfg.token) return;
+    if(!confirm('فقط اطلاعات کارت‌های بانکی از این نسخه تاریخی بازیابی شود؟ سایر اطلاعات فعلی دست‌نخورده می‌مانند.')) return;
+    createRecoverySnapshot('قبل از بازیابی کارت‌ها از تاریخچه GitHub');
+    const status=document.getElementById('backupHistoryStatus'); status.className='mini-status'; status.textContent='در حال دریافت کارت‌های نسخه انتخاب‌شده...';
+    try{
+      const api=githubApiBase(cfg);
+      const r=await fetchWithTimeout(`${api}?ref=${encodeURIComponent(commitSha)}`,{headers:githubHeaders(cfg),cache:'no-store'},10000);
+      if(!r.ok){ const ej=await r.json().catch(()=>({})); throw new Error(r.status+' '+(ej.message||'')); }
+      const j=await r.json(); const data=JSON.parse(base64ToUtf8(j.content));
+      if(!(data.cardsBlob&&data.cardsBlob.salt&&data.cardsBlob.iv&&data.cardsBlob.data)) throw new Error('این نسخه کارت بانکی در بکاپ ندارد');
+      restoreCardsBlobFromPayload(data.cardsBlob,true);
+      status.className='mini-status ok'; status.textContent='کارت‌های نسخه انتخاب‌شده بازیابی شدند. سایر داده‌ها تغییر نکردند. برای ثبت این وضعیت در GitHub، یک Push انجام دهید.';
+      render();
+    }catch(err){ status.className='mini-status warn'; status.textContent='بازیابی کارت‌ها ناموفق بود: '+((err&&err.message)||'خطای نامشخص'); }
+  }
+  document.getElementById('btnGithubHistory').onclick=()=>{ backupHistoryOverlay.classList.add('show'); loadGithubBackupHistory(); };
+  document.getElementById('btnReloadBackupHistory').onclick=loadGithubBackupHistory;
+  document.getElementById('btnCloseBackupHistory').onclick=()=>backupHistoryOverlay.classList.remove('show');
+  backupHistoryOverlay.addEventListener('click',e=>{
+    if(e.target===backupHistoryOverlay){ backupHistoryOverlay.classList.remove('show'); return; }
+    const b=e.target.closest('button[data-history-action="restore-cards"]'); if(b) restoreCardsFromHistory(b.dataset.sha);
+  });
+
   async function autoSyncOnReconnect(){
     if(autoSyncRunning || !isAutoSyncEnabled()) return;
     const m=loadSyncMeta(), cfg=loadGithubConfig(); if(m.pendingChanges<=0 || m.conflict || !cfg.repo || !cfg.token) return;
@@ -5098,8 +5177,13 @@
       return;
     }
 
+    const trashCount = cardsPlain.filter(c => c.deletedAt).length;
     bar.innerHTML = `<span>🔓 کارت‌های بانکی <b>باز</b> است — روی هر قسط، اطلاعات کارت‌های مرتبط دیده می‌شود.</span>
-      <div class="lockbar-row"><button class="ghost small" id="btnLockCards">🔒 قفل کردن</button></div>`;
+      <div class="lockbar-row">
+        ${trashCount ? `<button class="ghost small" id="btnOpenCardTrash">🗑 حذف‌شده‌ها (${toFa(trashCount)})</button>` : ''}
+        <button class="ghost small" id="btnLockCards">🔒 قفل کردن</button>
+      </div>`;
+    const trashBtn=document.getElementById('btnOpenCardTrash'); if(trashBtn) trashBtn.onclick=()=>{ renderCardTrash(); document.getElementById('cardTrashOverlay').classList.add('show'); };
     document.getElementById('btnLockCards').onclick = () => {
       cardsPlain = null; sessionPassphrase = null; revealed = {}; cardsRestorePendingNotice = false;
       render();
@@ -5111,7 +5195,7 @@
     const area = document.getElementById('orphanCardsArea');
     if (cardsPlain === null){ block.style.display = 'none'; return; }
     const loanIds = new Set(state.loans.map(l => l.id));
-    const orphans = cardsPlain.filter(c => !c.loanId || !loanIds.has(c.loanId));
+    const orphans = cardsPlain.filter(c => !c.deletedAt && (!c.loanId || !loanIds.has(c.loanId)));
     if (orphans.length === 0){ block.style.display = 'none'; return; }
     block.style.display = '';
     let html = '';
@@ -5152,6 +5236,31 @@
     });
   }
 
+  function renderCardTrash(){
+    const list=document.getElementById('cardTrashList');
+    if(!list) return;
+    if(cardsPlain===null){ list.innerHTML='<div class="mini-status warn">ابتدا کارت‌ها را باز کنید.</div>'; return; }
+    const deleted=cardsPlain.filter(c=>c.deletedAt).sort((a,b)=>String(b.deletedAt).localeCompare(String(a.deletedAt)));
+    if(!deleted.length){ list.innerHTML='<div class="mini-status ok">سطل زباله کارت‌ها خالی است.</div>'; return; }
+    list.innerHTML=deleted.map(c=>`<div style="padding:10px 0; border-bottom:1px dashed var(--paper-line); display:flex; gap:10px; align-items:center; flex-wrap:wrap;">
+      <div style="flex:1; min-width:240px;"><b>${escapeHtml(c.bank||'کارت')}</b> · <span class="mono">${maskNumber(c.number||'')}</span>${c.holder?` · ${escapeHtml(c.holder)}`:''}<div style="font-size:10px; color:var(--ink-soft);">حذف: ${escapeHtml(new Date(c.deletedAt).toLocaleString('fa-IR'))}</div></div>
+      <button class="primary small" data-trash-action="restore" data-id="${escapeHtml(c.id)}">↩️ بازیابی</button>
+      <button class="ghost small" data-trash-action="purge" data-id="${escapeHtml(c.id)}">حذف قطعی</button>
+    </div>`).join('');
+  }
+  document.getElementById('btnCloseCardTrash').onclick=()=>document.getElementById('cardTrashOverlay').classList.remove('show');
+  document.getElementById('cardTrashOverlay').addEventListener('click', async e=>{
+    if(e.target===document.getElementById('cardTrashOverlay')){ e.currentTarget.classList.remove('show'); return; }
+    const btn=e.target.closest('button[data-trash-action]'); if(!btn || cardsPlain===null) return;
+    const c=cardsPlain.find(x=>x.id===btn.dataset.id); if(!c) return;
+    if(btn.dataset.trashAction==='restore'){
+      delete c.deletedAt; await persistCards(); render(); renderCardTrash(); toast('کارت بازیابی شد');
+    } else if(btn.dataset.trashAction==='purge'){
+      if(!confirm('این کارت برای همیشه حذف شود؟ برای بازیابی بعدی فقط تاریخچه قدیمی GitHub ممکن است کمک کند.')) return;
+      cardsPlain=cardsPlain.filter(x=>x.id!==c.id); await persistCards(); render(); renderCardTrash(); toast('کارت برای همیشه حذف شد');
+    }
+  });
+
   const cardOverlay = document.getElementById('cardOverlay');
   let editingCardLoanId = null;
   function openCardModal(c, loan){
@@ -5198,11 +5307,14 @@
   };
   document.getElementById('btnDeleteCard').onclick = async () => {
     if (!editingCardId) return;
-    if (!confirm('این کارت حذف شود؟')) return;
-    cardsPlain = cardsPlain.filter(x => x.id !== editingCardId);
+    if (!confirm('این کارت به سطل زباله منتقل شود؟ بعداً می‌توانید آن را برگردانید.')) return;
+    const c = cardsPlain.find(x => x.id === editingCardId);
+    if (!c) return;
+    c.deletedAt = new Date().toISOString();
     await persistCards();
     closeCardModal();
     render();
+    toast('کارت به سطل زباله منتقل شد');
   };
   cardOverlay.addEventListener('click', (e) => { if (e.target === cardOverlay) cardOverlay.classList.remove('show'); });
 
