(()=>{const D=()=>window.GhestbanNextDomain.state();function metrics(){const s=D(),tx=s.transactions||[],loans=s.loans||[];const income=tx.filter(x=>x.type==='income').reduce((a,x)=>a+(+x.amount||0),0),expense=tx.filter(x=>x.type==='expense').reduce((a,x)=>a+(+x.amount||0),0);return{income,expense,net:income-expense,loans:loans.length,pending:tx.filter(x=>x.status==='pending').length}}window.GhestbanNextApp={metrics};})();